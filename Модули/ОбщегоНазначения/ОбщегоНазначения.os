Процедура УдалитьФайлыИзКаталога(КаталогВыгрузки) Экспорт
	
	МассивФайлов = НайтиФайлы(КаталогВыгрузки);
	Для Каждого Файл Из МассивФайлов Цикл
		Сообщить(файл.ПолноеИмя);
		УдалитьФайлы(Файл.ПолноеИмя);

	КонецЦикла;
	
КонецПроцедуры